# 相框风格分析与模板更新记录

> 样本来源：`Desktop/相框样式`（10 张用户提供的成品图）
> 分析日期：2026-08-28 · 配套实现：`infoLayout` 布局预设 + 3 个新内置模板
> 结论先行：样本归纳为 **A/B/C 三类风格**；A、B 已完整落地为内置模板，C（竖排杂志式）需竖排文字能力，暂不入库（见「已知边界」）。

---

## 一、风格分类与视觉特征

### 风格 A：白底杂志双栏式（样本 5/10 —— 主流形态）

样本：海鸥（Canon）、广州夜景·署名（Nikon）、日本车站（LUMIX）、橘猫竖版（XIAOMI）、伯劳（Nikon COOLPIX）

| 维度 | 特征 |
|---|---|
| 相框 | 纯白底；四边等宽留白（约画布宽 5%）+ **底部加宽约 2.2×**；照片直角（无圆角）；极淡或无阴影 |
| 信息排布 | **左右双栏三段式**，整体贴底单行带：左段 = 两行文本（主行粗黑 / 副行灰细）；中段 = 品牌 Logo 字标；**竖分隔线**（浅灰 1px）；右段 = 两行（参数行粗黑 + 日期行灰细） |
| 内容组合 | 组合1：镜头型号（粗）/ 机身（灰）；组合2：机身（粗）/ 镜头（灰）；组合3：头像 Logo + 署名（粗） |
| 参数格式 | `46mm f/1.6 1/60s ISO250`（焦距→光圈→快门→ISO，快门含 s） |
| 日期格式 | `2025-12-11 12:50` 或 `2026.06.10 18:35`（YYYY-MM-DD / 点分两种） |
| 变体 | 右栏单行（无日期行，参数行垂直居中）→ 极简署名版 |
| 适用场景 | 机型/镜头展示、署名水印、社交平台发图的标准形态 |

### 风格 B：圆角悬浮·模糊延展式（样本 3/10）

样本：飞机与月（Nikon 白字）、珠海城市（SONY）、狮子（SONY 竖版）

| 维度 | 特征 |
|---|---|
| 相框 | 照片**铺满画布**，四周为原图模糊延展（窄边，无额外压暗）；**照片大圆角**（约 40–60px）+ 投影悬浮感 |
| 信息排布 | 底部模糊区内**居中双行白字**：第一行 = 品牌字标（SONY/Nikon）+ 空格 + 机型（同 行混排，字标粗、机型细）；第二行 = 参数行 |
| 字号层级 | 明显小于风格 A（约为其一半），细字重 |
| 适用场景 | 想保留画面完整感、壁纸式分享 |

### 风格 C：极简杂志竖排式（样本 2/10）

样本：夜海、云倒影（均为胶片摄影签名款）

| 维度 | 特征 |
|---|---|
| 相框 | 照片占右侧约 78%（横图旋转 90° 竖放或竖图直放），**左侧白色竖条** |
| 信息排布 | 左条内**竖排文字**：顶部手写花体签名；参数列「值（粗大）+ 单位（细小）」成组竖排，组间竖线分隔：`23 mm ｜ F 1.8 ｜ 800 ISO ｜ 1/49 s` |
| 独特点 | 无品牌 Logo、无日期；签名即标识；文字旋转 90° 排布 |
| 适用场景 | 胶片感/艺术向、杂志内页式排版 |

---

## 二、INFO 信息模板规范（从样本提炼的标准）

1. **字体**：全样本为无衬线体；品牌标识用官方字标图（Canon 红 / Nikon 白斜体 / LUMIX / Leica 红圆标 / SONY 衬线），不用普通文本模拟品牌名。
2. **信息层级**（两级制）：
   - 主行 = 粗体、深色（近黑 #1a1a1a）：参数行 或 镜头/机型 或 署名
   - 副行 = 细体、灰色（约 #9a9a9a）：日期 或 机型
3. **参数顺序**（固定）：`焦距mm → 光圈 f/x → 快门（含 s）→ ISO`，单空格分隔。
4. **日期格式**：`YYYY-MM-DD HH:mm`（推荐，dateFormat=dash）或 `YYYY/MM/DD HH:mm`、`YYYY.MM.DD HH:mm`；精确到分。
5. **布局对齐**：A = 左段左对齐 + 右段左对齐（同一竖直起点）+ Logo 居中；B = 全部居中。
6. **分隔系统**：双栏之间用 1px 竖线，颜色随底色自适应（浅底 rgba(0,0,0,0.18)），线高 = 右栏两行高 + 余量。
7. **留白**：A 底部留白 ≈ 2.2× 侧边；B 底部模糊边 ≈ 68 设计 px（侧边 56）。
8. **文字颜色自适应**：随背景自动切换——纯色浅底 → 黑字（rgba(0,0,0,0.95)），模糊/照片/深底 → 白字（rgba(255,255,255,0.95)）；预览与导出同源生效（含型号行的灰细 = 主色 × cameraModelOpacity）。

---

## 三、FrameLab 能力映射与实现

| 样例特征 | FrameLab 实现 | 状态 |
|---|---|---|
| A 白底 + 底部加宽 | `bgMode=solid(#fff) + padding + bgBottomRatio` | 已有 |
| A 左右双栏 + Logo 居中 + 竖线 | **新增 `infoLayout: 'duo'`**（defaultPos/exporter 双端坐标 + 竖分隔线渲染） | ✅ 本轮新增 |
| A 左栏两行异构文本 | 型号块（cameraModel 元素，可 `\n` 手填两行）+ 灰细样式由 cameraModelOpacity/Weight 近似 | ✅（异构字重靠手动微调） |
| A 日期双格式 | dateFormat：date/datetime 已支持 `YYYY/MM/DD HH:mm`；点分 `.` 需手改文本 | ✅（点分为手填） |
| B 模糊延展 + 不压暗 | `bgMode=blur`（本轮已去除 brightness(0.7) 隐性压暗） | ✅ |
| B 大圆角 + 阴影 | `photoRadius + shadow` | 已有 |
| B 居中双行 | `overlayAlign=center + classic 堆叠`（logo 与机型同行 = 两元素并排拖拽定位） | ✅ |
| C 竖排文字 + 左白条 | 竖排渲染（writing-mode / 逐字旋转）+ 单位小字上下标 —— **当前不支持** | ❌ 已知边界 |

---

## 四、模板库更新记录（2026-08-28 / 2026-08-29 样例逐像素对齐重构）

用户收敛需求：**仅保留两张样例对应的模板**。已对两张样例（微信图片_20260828171406 / _20260828171422）做像素级测量并据此重写布局。

| 模板 | 类型 | 关键参数（实测换算） | 对应风格 |
|---|---|---|---|
| 白框参数卡（原 杂志双栏·白框参数卡） | 内置重调 | 白底 solid、**padding 27、bgBottomRatio 69、overlayBottom 18**、shadow 0.12、**infoLayout=duo**（左=镜头 20粗 + 机型 17灰0.55 / 中=Logo 20 / 右=参数 20粗 + 日期 17灰，右栏右缘对齐照片右缘内缩 21，竖线距文字 21、Logo 右缘距竖线 28）、日期 dash、**字号 20/粗 700**、镜头行开 | A 主形态（样例1） |
| 圆角悬浮·模糊延展 | 内置重调 | blur 60、**bgExpand 100、bgBottomRatio 90**（底部模糊条 190 容 info）、photoRadius 35、shadow 0.45、**infoLayout=inline**（行1 = Logo(白 20)+机型(20) 内联居中，行2 = 参数 18 居中）、无日期无镜头、overlayBottom 29、**logoColor #ffffff** | B（样例2） |
| 杂志双栏·极简署名 | **已删除**（用户仅需两种样式） | — | — |
| （现有）模糊背景 / 纯色边框 | 保留 | 不变 | — |

**能力新增（本轮）**：
- `FrameConfig.infoLayout: 'classic' | 'duo' | 'inline'`，新增 **inline 悬浮双行**预设与「信息布局」下拉选项。
- **core/infoLayout.ts 共享布局计算**：duo/inline 的默认坐标由预览（FooterInfo）与导出（exporter）共用同一函数（含 canvas measureText 量宽），保证两端逐像素一致；duo 右栏从「中线+90 固定值」改为**右缘对齐照片右缘**（随文本宽度自适应）。
- duo 下**日期行改用机型样式组**（灰细小字，与样例一致）；classic/inline 不变。
- 深色背景（blur/photo）下 INFO 文字与 Logo 增加**柔和投影**（预览 text-shadow + 导出 ctx.shadow 同参数）。
- exporter 修复：classic「镜头附加行」不再在 duo 下重复绘制（此前会与日期重叠）。
- useLogoStore 修复：**preloadBrandLogo 支持颜色缓存键**（此前导出着色 Logo 可能拿到空白占位画布）。

**样例实测几何基准**（设计 px，照片宽 1200 归一）：白卡 padding 27、底带 96、文字内缩 21、墨迹底边距 23、行墨迹距 14、参数/镜头字号 20(700)、机型/日期 17(400 #777)、竖线 #D3D3D3 距文字 21、Logo 高 20 距竖线 28；悬浮 顶/侧/底模糊带 61/145/190（取折中 100/100/190）、行1 Logo 20 + 机型 20 间距 35、行2 参数 18、行墨迹距 21、底边距 29。常量落位 `core/infoLayout.ts`。

**验证**：`verify-templates.html` 真机验收页（Vite 根路径直达）——真实照片（DJI 4:3→3:2 裁剪）经 exportFrame 导出后做 16 项像素断言，**16/16 ALL PASS**（含边框/白条高/内缩/底边距/行距/竖线间距/Logo 间距与高度/字高/居中/双行结构）；vue-tsc 0 错误、vitest 66/66 通过。

---

## 五、已知边界与后续建议

1. **风格 C 未支持**：需 INFO 层支持竖排文字（新元素类型 `vertical-text`：逐字旋转 90° + 值/单位双字号），建议与「分隔线元素化」一起排入阶段 17。
2. **inline 布局的镜头行**：inline 下镜头型号不参与默认排版（模板默认 `showLens=false`）；duo 下镜头行已参与左栏排版。
3. **duo/inline 坐标自适应**：右栏右缘对齐照片右缘（随文本宽度量宽自适应），无需手动微调；各元素仍可独立拖拽（拖后写坐标，resetInfo 可复位）。
