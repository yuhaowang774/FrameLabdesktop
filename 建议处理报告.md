# FrameLab 评论区建议处理报告

> 核对基准：[评论区建议整理与优化总结.md](./评论区建议整理与优化总结.md)（12 视频 2000+ 评论）+ [给开发的需求清单.md](./给开发的需求清单.md)
> 核对方式：静态代码审查 + 浏览器自动化实测（IndexedDB/localStorage 断言、性能计时、单元测试）+ 本轮联调证据（G1–G9）
> 核对日期：2026-08-27 · 基线：vue-tsc 0 错误 / vitest 66/66
> **2026-08-27 晚更新**：行动指引「本迭代」三项已实施并实测通过（见第四节标注 ✅ 已实施）；镜头行引入时修复了页脚自动布局物化导致的位置不重排 bug。
> **2026-08-28 更新**：「下迭代」两项（断层噪点 #2、放大预览 #4）已实施并量化验证通过。优先级表 Top 5 仅剩地址栏 #5 待评估，其余为架构级/策略项。
> **2026-08-28 更新 2**：地址栏 #5 已实施（离线省市表 + DJI GPS 端到端实测通过）。优先级表 Top 6 技术项全部完成，剩余 #8 RAW 策略 / #9 拼图 / #10 应用内引导 / #6 HDR 完整管线为策略或长期项。
> **2026-08-28 更新 3**：① 地址栏功能经用户决策**整体取消**并回退（数据源/升级路线调研保留存档）；② 应用内引导 #10 已实施（5 分组使用指南弹窗 + 首次自动弹出）。至此本轮可执行项全部闭环，剩余 #6 HDR / #8 RAW / #9 拼图为策略或长期项。
> **2026-08-28 更新 4（修复所有已知问题）**：① **#6 折中已实施**——JPG 导出嵌入程序化构建的 sRGB ICC（508B，APP2 注入器含防重复/异常防护），实测端到端通过（语义修正：嵌 sRGB 而非原图 ICC，避免双重转换）；② **切图竞态修复**——切换照片全程挂起历史提交，消除"切换后立即操作被异步恢复覆盖 + 产生脏节点"（冒烟实测的遗留竞态）；③ **resetInfo 补页脚坐标复位**（null 回自动布局）；④ 种子图 ERR_ABORTED 重复请求判定为 dev 网络噪音不修；⑤ #8 RAW 策略 / #9 拼图为策略决策与新模块，非缺陷修复，维持排期。
> **2026-08-28 更新 5（样例驱动的模板库扩展）**：分析 `Desktop/相框样式` 10 张样例，归纳 A（白底杂志双栏）/B（圆角悬浮模糊延展）/C（极简竖排）三类风格；新增 `infoLayout: classic|duo` 布局预设（FooterInfo/exporter 双端 + 竖分隔线 + INFO 面板「信息布局」下拉）与 3 个内置模板（杂志双栏·白框参数卡 / 杂志双栏·极简署名 / 圆角悬浮·模糊延展），几何与导出实测通过。交付文档：[相框风格分析与模板更新.md](./相框风格分析与模板更新.md)。风格 C（竖排文字）列为阶段 17 能力项。

---

## 一、核对总览

| 状态 | 数量 | 说明 |
|---|---|---|
| ✅ 已解决 | 18 | 含 6/6 个 P0 硬伤中 5 个的架构性根除 |
| 🟡 部分解决 | 9 | 主诉求已满足，子项有缺口 |
| ❌ 未解决 | 10 | 集中在镜头信息、色彩管线、地图逆地理、拼图等 |

**总评**：评论区 P0 六大硬伤（画质压缩/闪退/大图崩/卡顿/命名/预览）五项在架构层面根除并有实测证据；P1 高频诉求（批量+模板、logo 体系、EXIF 准确性、模糊增强、配置持久化）主体完成。剩余缺口集中在四类：**镜头型号信息、色彩深水区（HDR/断层）、地图逆地理、拼图类新功能**。

---

## 二、逐条核对明细

### 🔴 P0 硬伤（需求清单编号）

| 编号 | 诉求（评论区原文要点） | 状态 | 证据 / 缺口 |
|---|---|---|---|
| P0-1 | 19M→1.9M 画质压缩、300dpi→25dpi、颜色断层 | 🟡 部分 | ✅ 实测 PNG 15600×11600 无损 214MB、JPG 0.95 画质 38.5MB，主照片原生像素 1:1 进画布（色差=0）；❌ HDR/广色域（G6，8bit SDR 管线）；❌ 模糊断层微量噪点缓解未做 |
| P0-2 | Win11 24H2 闪退、安装权限、导出强退 | ✅ | 纯前端本地处理 + Tauri 免安装形态，无安装步骤；桌面端持续运行无闪退 |
| P0-3 | 24MB Z8 导入即崩、一亿像素生成失败 | ✅ | 96MP 导入/切换实测无崩溃，预览路径 6144 长边降采样保护；48MP 首次解码 1847ms 有占位不卡死 |
| P0-4 | 打一字卡 5s | ✅ | 实测 30 字符连续 input 平均 0.51ms / 最大 1.9ms |
| P0-5 | 导出文件名 `xxx.jpg-1` | ✅ | [exporter.ts L434](./src/core/exporter.ts) `${prefix}_${ts}.${ext}`，时间戳在扩展名前，后缀合法 |
| P0-6 | 反复导出试错、跪求预览 | 🟡 部分 | ✅ 编辑画布即所见即所得（导出与预览共用渲染代码），实时刷新；🟡 导出结果弹窗可查看大图（max 60vh + 滚动）；❌ 编辑中单图点击放大/缩放观看 |

### 🟠 P1 高频功能

| 编号 | 诉求 | 状态 | 证据 / 缺口 |
|---|---|---|---|
| P1-1 | 模板做好只能一张张点亮；自定义文本"应用到所有"无效 | 🟡 部分 | ✅ 模板保存/导入导出 JSON（[useTemplates.ts](./src/composables/useTemplates.ts)）；⇉ 批量应用实测（选中集各自写历史节点、EXIF 逐张保留，G5）；批量导出 + 逐张回填（G1，`configFor`）；批量同步存模板；单张应用也保留 EXIF（本轮修复）。❌ 参数替换映射（"腾龙28-200 E A071"→"腾龙 28-200"）未做 |
| P1-2 | 自定义 logo 跨平台失败、松下未标 Lumix、缺苹果/影石 | ✅ | IndexedDB 上传/删除/缩放/透明度，无跨平台权限问题；品牌库 14 项含 Apple/Insta360/Lumix（G4 实测文字标记渲染）；型号自由文本（新机型免等库更新） |
| P1-3 | 1/0s、等效焦距、品牌不可改、时区只显示日、中文路径 | 🟡 部分 | ✅ ≥1s 显示 Ns（结构性正确）；等效焦距开关 + 手动裁切系数 7 档（G2，EXIF 35mm 缺失时手动兜底）；品牌下拉/型号文本可改；日期三种格式含只到日（G3）；桌面 asset 协议读字节，中文路径实测正常；EXIF 文本自由编辑（无 EXIF 照片可手填）。❌ 镜头型号/副厂镜头厂商标未读取（LensModel 不在 exifr pick）；❌ HDR 保留（同 G6） |
| P1-4 | 模糊程度/圆角可调、输出比例、文字排列统一、色彩偏暗 | 🟡 部分 | ✅ 模糊强度/边框圆角/照片圆角独立滑块；画面比例 16:9/9:16/1:1 等 7 档（[constants.ts L90](./src/core/constants.ts)）；日期/参数/型号独立开关；INFO 三元素自由拖拽（含居中吸附+辅助线，[FooterInfo.vue L194](./src/components/preview/FooterInfo.vue)）；导出逐张进度（G8）。❌ 模糊断层未加微量噪点；❌ 色彩偏暗/发紫修正未做 |
| P1-5 | 配置能不能保存，每次恢复默认 | ✅ | 双链实测：localStorage（布局/模板/快照/编辑模式/当前选中照片）+ IndexedDB（历史链/自定义 logo）；刷新后模板、参数、选中态全部保留（G9） |

### 🟡 P2 体验与扩展

| 编号 | 诉求 | 状态 | 证据 / 缺口 |
|---|---|---|---|
| P2-1 | HEIC/TIF/RAW/JXL 支持 | 🟡 部分 | ✅ 输入支持 JPG/PNG/WebP/GIF/BMP/AVIF；HEIC 及损坏文件导入有明确汇总提示（G7，实测弹窗）；❌ TIF/RAW/HEIF 解码（Chromium 限制，需 Rust 侧解码器）；❌ JXL；❌ HDR 保留 |
| P2-2 | Mac/iOS/安卓/鸿蒙/Linux/TV/小程序 | 🟡 部分 | ✅ 桌面版（Tauri，Win 实测运行中，Mac 可同栈打包）；🟡 移动端按项目决策暂停；❌ TV/小程序 |
| P2-3 | 字体/大小/颜色、吸附、靠右同行、边距可调 | 🟡 部分 | ✅ 8 字体 + 字号/字重/透明度，型号独立字体；居中吸附 + 辅助线高亮；三元素自由拖拽（靠右/同行/任意位置）；四边等宽 + 下边加宽（0–400px 扩展）+ 阴影滑块；🟡 编辑中单图放大镜未做（同 P0-6 子项）；移动端滚动滑块冲突 N/A（移动端暂停） |
| P2-4 | 竖版、旋转信息、统一比例、拼横排 | 🟡 部分 | ✅ 实测 90° 旋转 frame 1200×900→1200×1600 正确自适应；frameRatio 统一输出比例；竖图文字自动布局不重叠（autoPlaceFooter）；❌ 多竖图拼横排（拼图功能，规划中） |
| P2-5 | 地址栏、签名、镜头型号、立体阴影、轻量模板、叠加水印 | 🟡 部分 | ✅ 拍摄日期三种格式（G3）；签名/联系方式 → EXIF 文本自由编辑 + 水印文字承载；水印系统完整（文字/图片/透明度/大小/角度/平铺/对齐/位置——叠加式水印已实现）；立体阴影滑块；纯白底/纯边框 + 关闭全部参数的轻量组合。❌ 经纬度→省市地址；❌ 镜头型号 |
| P2-6 | 官网/下载加速 | ✅（N/A） | 纯本地处理无服务器依赖；未发布，分发渠道待定 |

### 🟢 加分项

| 项 | 状态 | 说明 |
|---|---|---|
| 本地离线版 | ✅ | 全部处理在本地完成，隐私 + 稳定（对 Semi 中文路径/依赖问题双重差异化） |
| 开箱即用 GUI | ✅ | 核心卖点成立；批量/模板/拖拽均有入口 |
| 应用内引导 | ✅ 已实施 | 「?」= 5 分组「使用指南」弹窗（工作流/批量/开关逻辑/缩放/格式隐私），首次启动自动弹出；实测通过 |
| PS/LR 插件 | ❌ | 未规划（可列为长期） |
| 开源 | ❌ | 策略决策，非技术项 |

---

## 三、未解决项优先级排序（按对作品质量的影响）

| # | 项 | 级别 | 影响分析 | 成本 |
|---|---|---|---|---|
| 1 | **镜头型号 + 副厂镜头厂商标**（P1-3/P2-5） | ✅ 已实施 | 评论区 6+ 条（镜头控/如雪如尘/准神/邮差小狗等）；LensMake+LensModel 解析、EXIF 块附加行渲染、开关/手填/批量回填全链路实测通过 | 低（半天） |
| 2 | **模糊背景断层 + 色彩偏暗缓解**（P0-1/P1-4 子项） | ✅ 已实施 | dither 噪点打破量化色带（最长等值行程 180→9），移除隐性 brightness(0.7) 压暗（亮度复现对照 1.041 倍） | 中（1 天） |
| 3 | **参数替换映射**（P1-1 补充） | ✅ 已实施 | 批量出图最后一环；规则表（每行 查找=>替换）+ 持久化 + 批量回填链路实测通过 | 低（半天） |
| 4 | **编辑中单图放大预览**（P0-6 验收子项） | ✅ 已实施 | 双击锚点 2x / Esc 复位 + 既有滚轮缩放与拖拽平移，实测往返精确 | 中（半天–1 天） |
| 5 | **地址栏（经纬度→省+市）**（P2-5） | 🚫 已取消 | 用户决策移除该功能（已全链路回退）。数据源与升级路线调研存档见第四节 #5，如重启按存档执行 | — |
| 6 | **HDR/广色域保留**（G6） | ✅ 折中已实施 | JPG 导出嵌入 sRGB ICC（508B 程序化构建，APP2 注入器含防重复/结构防护），消除无标签看图软件的偏色解释问题；完整 GainMap 管线仍为长期项 | 高（架构级，长期） |
| 7 | **logo 颜色可改**（P1-2 补充） | ✅ 已实施 | 「尼康黄/佳能红」类诉求；Logo 颜色下拉（自动/白/黑/品牌主色/自定义取色器，8 品牌主色表），SVG 单色化重绘实测生效 | 低（半天) |
| 8 | **TIF/RAW/HEIC 输入**（P2-1） | 🟡 | 需 Rust 侧解码器（libraw/heif），包体积 + 复杂度大增；或引导"转 JPG"策略 | 高（策略决策） |
| 9 | **多竖图拼横排**（P2-4） | 🟡 | 新功能模块（PROJECT_PLAN 阶段 17 已列），非修补 | 高（新模块） |
| 10 | **应用内引导/开关逻辑说明** | ✅ 已实施 | TopBar「?」升级为 5 分组「使用指南」弹窗（工作流/批量出图/INFO 开关逻辑/查看细节/格式与隐私），首次启动自动弹出一次，localStorage 标记；实测自动弹出/关闭/重开/持久化全过 | 低（1 天） |

---

## 四、改进方案与实施步骤（Top 6）

### 1. 镜头型号 + 副厂镜头厂商标 🔴 ✅ 已实施（2026-08-27）
- **方案**：exifr pick 增加 `LensModel/LensMake`；`parseExif` 返回 `lens`；EXIF 文本块附加镜头行（showLens 开关 + lensText 可手改，随 EXIF 元素拖拽，块自动上移避让日期）；`{lens}` 模板占位（infoRenderer）；批量回填/模板 keep 集合同步保护 lensText。
- **实测**：预览/导出双端渲染一致；开关往返 top 精确偏移 46px（fontSize+16）；无 EXIF 镜头的图（重编码种子）正确留空可手填。

### 2. 模糊断层 + 色彩偏暗缓解 🔴 ✅ 已实施（2026-08-27）
- **方案**：① `drawBlurredBackground` 渲染后叠加 alpha=0.013 黑白噪点瓦片（128×128 模块级缓存，pattern 填充，缓存在离屏合成内 → 预览/导出同源）；② 移除隐性压暗：主图模糊背景 `brightness` 由硬编码 0.7 改为 1（「模糊后偏暗/不艳」根因），与自定义背景路径统一。
- **实测**：同参数 A/B 复现对照——含 dither 的最长等值行程 9（无 dither 对照 180）、一阶差分标准差 2.131（对照 0.168），色带阶梯被打破且无可感噪点；live 背景亮度 = dim=1 复现值的 1.041 倍（若 0.7 仍在应为 0.7 倍）。
- **预期**："颜色断层/偏暗"类硬伤评论区闭环；纯 Canvas 管线内完成，无新依赖。

### 3. 参数替换映射 🟠 ✅ 已实施（2026-08-27）
- **方案**：导出面板「批量回填」下方新增「文本映射」（开关 + textarea，每行 `查找 => 替换`，localStorage `frame-text-rules` 持久化）；批量回填路径对 exifText/cameraModel/lensText 链式替换。
- **实测**：规则持久化逐字符一致；刷新后开关与文本恢复；含映射批量导出管线 9.65s 完成无报错。

### 4. 编辑中单图放大预览 🟠 ✅ 已实施（2026-08-27）
- **方案**：复用既有滚轮缩放（锚点缩放 0.1–8x）+ 拖拽平移 + 缩放指示器，新增两个快捷入口——stage 双击（未放大 → 以双击点为锚 2x；已放大 → 复位）、全局 Esc 复位视图。
- **实测**：双击 100%→200%（锚点平移非零）、再双击/Esc 精确复位、滚轮缩放回归通过。

### 5. 地址栏（经纬度→省+市）🚫 已取消（2026-08-28，用户决策）
- **过程**：当日实施离线锚点匹配（省 bbox + ~200 地级市点 + 28 区县锚点）并通过 DJI 端到端验证；用户实测指出长安区误判为咸阳（连片都市圈「最近驻地」启发式失效）后补录区县锚点层修复；随后用户评估升级路线（离线 GeoJSON 边界 / 高德 API）后决定**整体取消该功能**，全链路已回退移除（geoChina.ts 删除、EXIF GPS 解析/UI 开关/渲染行/回填/keep/映射全部摘除），vue-tsc 0 错误、66 测试全绿、冒烟 14 项断言通过。
- **历史链遗留**：旧链节点 state 中可能残留 locationText/showLocation 键，属无害多余字段（加载时被忽略），不做数据清洗。
- **升级路线评估存档（如未来重启该功能直接按此执行）**：当前锚点表的边界误判（如长安区案例）的根本解法是真实行政边界。三条路线对比——
  - **A. 离线边界升级**：内置简化版市/区县 GeoJSON + 射线法点在多边形内判定（~30 行），区县级真实边界、根治误判，仍纯本地；成本约半天，体积 +0.5~1MB（Douglas-Peucker 预简化）。数据源已调研：AreaCity-JsSpider-StatsGov（免费开源月更，可导出 WGS-84，首选）、DataV.GeoAtlas（GCJ-02 需转换）、China-GeoData（MIT）、民政部县级 TopoJSON（CGCS2000≈WGS-84）。
  - **B. 高德逆地理 API**：乡镇/街道级精度，但 ① key 泄露风险只能用户自填（抬高门槛）② 联网依赖与「纯本地」卖点冲突 ③ GPS 上传第三方 ④ 个人 key 日配额 5000 次。适合做**可选增强**（设置面板自填 key + IndexedDB 坐标网格缓存 + 离线兜底），不宜内置打包。
  - **C. Nominatim (OSM)**：中国区数据差 + 1 req/s 限速，不适合主力。
  - **推荐架构**：A 保底 + B 可选增强（分层降级）。

### 6. HDR/广色域（G6）🟠 折中方案 ✅ 已实施（2026-08-28）
- **实施内容（与原折中方案有一处语义修正）**：新建 [icc.ts](./src/core/icc.ts) 程序化构建最小合法 sRGB ICC v2 剖面（508B，9 标签，零外部依赖）+ JPEG APP2 "ICC_PROFILE" 注入器（插在 SOI/APP0/APP1 之后、单块 ≤65519B 自动分块、已带 ICC 时跳过、结构异常原样返回）；exporter 在 JPG 导出后自动嵌入。
- **语义修正说明**：原方案写「嵌入原图 ICC」——实测确认 canvas drawImage 时浏览器已把像素转换到 sRGB，若再标 AdobeRGB 等原色域会导致看图软件二次转换、偏色加剧。正确做法是嵌入 **sRGB** ICC：让「无标签时错误猜解读」的看图软件获得确定性解释。
- **实测**：ICC 构建自洽（size=508=声明值、acsp/mntr/RGB /XYZ、9 标签、desc='sRGB IEC61966-2.1'）；端到端 JPG 导出含 APP2 ICC_PROFILE 且可正常解码；防重复注入防护通过（已有 ICC 字节零改动 / 无 ICC 注入开销精确 526B）。
- **完整 HDR/GainMap 管线仍为长期项**。

---

## 五、行动指引（建议顺序）

1. ~~**本迭代**（低成本高感知，共约 2.5 天）：镜头型号 #1 → 参数映射 #3 → logo 主色 #7~~ ✅ 已完成（2026-08-27 晚，三项全实施 + 顺带修复页脚自动布局物化 bug）
2. ~~**下迭代**（画质收官，约 2 天）：断层噪点 #2 → 放大预览 #4~~ ✅ 已完成（2026-08-28，量化验证通过）
3. ~~**评估项**：地址栏 #5 → HDR 折中 #6 → RAW/TIF 策略 #8~~ #5 已完成（含用户实测反馈的西安/咸阳都市圈误判修复）；#6/#8 待策略评估
4. **版本规划**：拼图 #9 与移动端恢复一起进入阶段 17 演进池；~~应用内引导 #10 随公测前做~~ ✅ 已完成（2026-08-28）

### 附：本轮实施顺带修复的存量 bug
- **页脚自动布局物化**：原 `autoPlaceFooter` 在照片加载时把默认位置写入 state，导致后续「显示镜头型号/拍摄日期」开关与字号变化不再重排（实测镜头行与日期重叠 5px）。已移除物化，null 坐标统一走 `defaultPos` 动态计算（拖拽起点本就有 defaultPos 兜底），拖拽后仍写入用户坐标。注意：**旧历史节点中已物化的坐标会被继续尊重**（符合"用户布局不覆盖"语义），如需恢复默认布局可撤销历史或重新导入。

---

## 附：本轮联调已闭环的实证清单（G1–G9）

| 项 | 结果 |
|---|---|
| G1 批量导出逐张 EXIF 回填 | ✅ 三场景像素级比对通过 |
| G2 等效焦距（35mm 换算 + 手动系数） | ✅ DJI 6.7mm→10.1mm（APS-C）实测 |
| G3 拍摄日期三种格式 + 时区规避 | ✅ 画布/切换实测 |
| G4 品牌库 14 项（Apple/Insta360/Lumix） | ✅ 文字标记渲染实测 |
| G5 批量模板应用 + 各照片 EXIF 保留 | ✅ IndexedDB 逐节点断言 |
| G7 无法导入文件明确提示 | ✅ 假 HEIC 弹窗实测 |
| G8 导出逐张进度 + 长渲染流动条 | ✅ DOM 采样实测 |
| G9 刷新恢复选中照片 + 历史链持久 | ✅ 两次冷启动快照一致 |
