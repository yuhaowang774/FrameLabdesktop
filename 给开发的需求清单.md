# 给开发的需求清单（壹印 / FrameXT 评论区建议压缩版）

> 数据来源：12 个 B站评论区 CSV（壹印 v1.3/v1.4/v1.5/基础版、Semi-Utils 同类工具、Copicseal 开源、磨砂相框工具等，共约 2000+ 条评论）
> 说明：按 P0（必须修）→ P2（扩展）排列，每条给出「问题 / 需求 / 验收要点」。

---

## 🔴 P0 —— 必须尽快修的硬伤

### P0-1 导出画质压缩严重
- **问题**：体积/分辨率/色彩被明显压低，用户感知"糊、偏色、变 SDR"。
- **需求**：原画质 / 质量可选 / 无损输出。
- **验收**：高像素机型放开 PIL 像素上限（8900 万）；保 8bit→可选项深；sRGB 默认避免偏色、支持 AdobeRGB/DisplayP3；模糊断层处加微量噪点；保留 HDR。

### P0-2 安装 / 启动 / 闪退
- **问题**：Win11 24H2 双击无反应、直接闪退、导出强退；装 C 盘/选用户权限导致打不开。
- **需求**：免安装绿色版 + 启动崩溃日志自动收集 + 旧版升级兼容。
- **验收**：默认"全用户"安装；安装失败有日志落盘（Roaming/yiyin）；旧版覆盖安装不冲突。

### P0-3 大图闪退 & 卡加载
- **问题**：20M+ / RAW / LR 转出 jpg 导入即闪退；启动卡"加载中"。
- **需求**：图片解码与渲染优化、懒加载、进度反馈。
- **验收**：单张 ≥50MB 不闪退；导入列表可分批渲染。

### P0-4 编辑文本 / 参数卡死
- **问题**："打一个字卡 5 秒"，UI 主线程阻塞。
- **需求**：输入与渲染分离（异步/后台线程）。
- **验收**：连续打字无卡顿；参数改动不阻塞主线程。

### P0-5 导出命名 bug
- **问题**：`xxx.jpg-1` 破坏后缀、日期编辑光标错乱。
- **需求**：编号追加在扩展名前（`xxx-1.jpg`）；日期输入框光标稳定。
- **验收**：批量导出文件名合法、可被系统识别。

### P0-6 结果实时预览缺失（极高频）
- **问题**：用户反复导出试错，效率低。
- **需求**：加半实时 / 结果预览（所见即所得）。
- **验收**：改参数后预览图即时刷新；支持单图放大查看。

---

## 🟠 P1 —— 高频功能缺失

### P1-1 真正的批量 + 模板保存应用（被提最多）
- **需求**：自定义模板（边框/logo/文字/颜色/字体）保存并一键应用到全部照片；
- **补充**：无 EXIF 照片自动分流；参数替换映射（批处理不同镜头/机型）。
- **验收**：模板可导出/导入；批量 100 张内不卡死。

### P1-2 自定义 logo 跨平台稳定
- **需求**：支持删除/缩放/PNG；Mac 不再报错、Win 免管理员；
- **补充**：修正品牌库（松下=Lumix）；补 苹果/影石/哈苏/徕卡/大疆 等；新机型库及时更新（Z50 二代等）；logo 颜色可改。
- **验收**：Mac/Win 双端 logo 加载一致；品牌识别准确。

### P1-3 EXIF 准确性
- **需求**：
  - 长曝光快门 >1s 不再显示 `1/0s`（应显示 Ns）；
  - 焦段识别错误（读错原始值）修正；
  - 等效焦距换算（m43/aps-c/s35/中画幅下拉）；
  - 镜头 logo 缺失只标焦段 → 补副厂/手动头厂商标（腾龙/适马/永诺/福伦达/理光GR3）；尼克尔字段不再隐藏；
  - 修图/裁切/文件夹空格/中文路径导致 EXIF 丢失 → 兼容；
  - HDR 保留（加完不变 SDR）；
  - 时区错误只显示秒 → 支持只显示到日；
  - 品牌名可手动改；日期显示到日可配置。
- **验收**：各类机型 EXIF 读取一致；中文路径正常。

### P1-4 高斯模糊 / 背景模糊模板增强
- **需求**：模糊程度 / 圆角数值可调；输出比例可调；日期/参数开关；去参数；统一文字排列；超分崩溃修复；模糊后色彩偏暗修正。
- **验收**：模糊参数实时预览；超分大图不崩。

### P1-5 配置持久化
- **需求**：保存用户配置（模板/质量/路径），避免每次恢复默认。
- **验收**：重开软件配置保留；不同用户互不覆盖。

---

## 🟡 P2 —— 体验与扩展

### P2-1 多格式支持
- HEIF/HEIC、PNG、TIF、RAW（ARW/CR/NEF/CR2/DNG）、JPEG XL、HDR 保留。
- 验收：至少新增 HEIF/HEIC 与 PNG 稳定支持。

### P2-2 全平台覆盖
- Mac/iOS 安装包、Android、鸿蒙、Linux、TV、小程序；离线本地版。
- 验收：提供 Mac 原生包；移动端至少出测试版。

### P2-3 界面交互
- 字体/大小/颜色选择；文字吸附+靠右同行；边距/四边等宽/留白可调；滚动与滑块冲突修复；统一各端导出/更新按钮；窗口控件适配；单图放大预览。

### P2-4 方向 / 比例 / 边框
- 竖版模板；旋转信息正确处理；竖图文字不重叠；按厚度统一输出比例；竖图转横（背景模糊）；多竖图拼横排。

### P2-5 附加信息项
- 地址栏（经纬度→省+市，可接高德 API）；签名；镜头型号；公司名/联系方式；立体阴影；纯白底/纯边框无参数轻量选项；叠加式（侵入画面）水印；简单模板降低信息过载感。

### P2-6 基础设施
- 官网稳定性与下载加速；网盘/离线包/GitHub 镜像多通道兜底。

---

## 🟢 加分项（差异化）
- PS / LR 插件形态
- 开源（参考 Semi-Utils / Copicseal 社区价值）
- 本地离线版（隐私/稳定）
- **开箱即用、无需命令行**（相对 Semi-Utils 的核心卖点；注意中文路径兼容）
- 应用内新功能引导/教程（解决"找不到批量/logo/竖版入口""开关逻辑误解"）

---

## 附：Semi-Utils / Copicseal 反向印证（避免重蹈覆辙）
- Semi-Utils 命令行版硬伤：中文/外语路径报错读不到 EXIF；格式极窄（raw/nef/rwl/png/dng/heif 报"只支持 jpg"）；缺 input/output 目录与 tqdm 依赖；副厂/手动镜头只标型号；死循环 bug（Mac 垃圾桶未清、0字节/损坏图、无 EXIF 图、输入 `y`）→ 已 build1517 修复。
- Copicseal 痛点：导出没反应、配置不保存、个人 logo 待补、字体单一、超分崩溃、GitHub 下载慢。
- 结论：GUI 开箱即用 + 中文路径兼容 + 配置持久化，是相对同类工具的核心优势，应守住。
