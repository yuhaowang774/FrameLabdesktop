# FrameLab 项目记忆（AGENTS.md）

## UI 设计要求（用户明确要求，长期有效）

- **界面中禁止使用彩色 Emoji 符号**（如 🔍 🕘 ★ 等装饰性 emoji）。需要图标时使用纯文本符号（现有惯例：✕ 关闭、⇉ 批量、✎ 重命名）或内联 SVG；搜索框等控件不放图标，用 placeholder 文案表达用途（如「搜索模板名称 / 说明…」）。
- 界面文案与注释使用中文；命名/标识符用英文。

## 竞品样式学习的合规边界（用户确认，长期有效）

学习竞品（如 FrameElf）模板样式时，遵守「学思路、不抄表达」：

- ✅ 允许：浏览公开页面观察风格；用概括性语言记录版式思路（竞争分析）；以自有引擎/参数/文案**自主实现**同风格范式的模板（通用版式不垄断）
- ❌ 禁止：逐像素复刻竞品具体模板；复制其代码/素材/图标/条形码图形/预览图；使用其名称（FrameElf / 边框水印精灵）或 UI 整体照搬；宣传物料出现「XX 同款 / 对标 XX」表述
- 实现新模板时示例占位文案用自主命名，不沿用竞品的具体创意文案；模板 desc 用风格描述而非竞品指向

## 设计决策记录

- 2026-09-13 模板中心改版（用户拍板）：TemplatePickerModal 采用「左侧分类侧栏 + 3 列瀑布流 + 底部操作栏」三段布局，**不设右栏大预览**；**弹窗铺满整个软件窗口（与界面同大，100% 宽高）**；卡片按模板真实比例展示（masonry），hover 浮层显示名称/说明；侧栏含「最近使用 / 我的模板 / 风格分类九组」。**交互流（用户拍板）：点击卡片即应用模板并立即关闭弹窗返回编辑界面；弹窗内不做任何批量应用入口（底栏按钮与卡片角标均已移除）；底栏无「完成」按钮；关闭入口 = 左上角「←」返回箭头或 Esc（不用叉号）**。参考竞品：Canva / 飞书模板中心 / VSCO 缩略图网格范式（调研结论见会话记录）。
- 2026-09-15 模板库交互改为「先预览、后应用」（用户拍板，**取代 09-13 的「点卡即应用」**）：点击卡片不再直接应用并关闭弹窗，而是弹出**预览确认层**（覆盖弹窗内容区：标题 + 大图 + 模板说明 + 「退回继续选择 / 确认应用」两个按钮），用**用户自己的照片 + 当前 INFO** 走与导出同源的合成管线渲染大图（长边 1600；未打开照片时回退该模板样张并在底部注明）。满意点「确认应用」才应用模板并关闭弹窗返回编辑；不满意点「退回继续选择」（或 Esc）回到列表继续挑。预览态 Enter = 确认应用；列表态 Esc = 关闭模板库（沿用 09-13）。**卡片不设 hover 信息浮层**（模板名称/说明只在预览确认层里呈现，列表即纯成片瀑布流）；瀑布流宽屏 4 列、响应式递减 3 / 2 / 1 列（09-15 用户最终确认）。
