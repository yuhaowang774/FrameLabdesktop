node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log
.gitmsg
verify-export.html
devserver.log
devserver.err

# Tauri
src-tauri/target
src-tauri/gen/schemas

# 构建产物（vue-tsc -b 输出）
*.tsbuildinfo
vite.config.js
vite.config.d.ts

release/

# 宣传页内嵌网页版（Pages 构建时生成）
website/app/

# 宣发清理归档（本地备份，不入库）
_archive/

# 宣发素材与工作文档（仅本地，不入库）
marketing/

# 模板画廊本地评审照片（桌面「模版照片」同步，约 200MB，仅本地不入库）
src/assets/gallery/

# 软件模板库同步快照（「我的模板」，仅本地评审用）
src/core/appTemplatesSync.ts
